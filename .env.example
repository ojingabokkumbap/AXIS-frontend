# API base (optional; defaults to /api proxy in dev)
# VITE_API_BASE_URL=https://api.axisexam.com

# PortOne V2 — NHN KCP virtual account (/apply Step 4). Same values as backend PORTONE_*.
VITE_PORTONE_STORE_ID=
VITE_PORTONE_CHANNEL_KEY=

# Set to "true" on staging for NHN KCP contract review (mock schedules + demo VA modal; no payment API).
# Omit or "false" in production.
# VITE_APPLY_KCP_DEMO=true
